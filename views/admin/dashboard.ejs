<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <%- include('../partials/adminNavPartial.ejs') %>
    <div class="contaniner">
        <div class="author">
            <h1 class="author-heading"><%= locals.title %></h1>
            <p class="author-body"><%= locals.description %></p>
           </div>
           <img src="/images/hero.jpg" alt="" width="980" height="528" class="main-img">
           <section class="articles">
            <h2 class="articles-heading">Latest Posts</h2>
            <ul class="article-ul">
                <% data.forEach((item) => { %>
                  <li>
                    <a href="">
                        <span class="article-title"><%= item.title %></span>
                        <span class="article-list-date"><%= item.createdAt %></span>
                    </a>
                </li>
                <% }) %>
               
            </ul>
            
           
          
           </section>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>
</body>
</html>